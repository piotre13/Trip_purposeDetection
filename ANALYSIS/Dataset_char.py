# -*- coding: utf-8 -*-
"""samanehProject.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1XNAztzGdebugCilzjvHlthMxbIjmepnD

**Install requirements**
"""

# Import the necessary packages
import pandas as pd
from google.colab import files

uploaded = files.upload()

# print the size and the name of the dataset
for fn in uploaded.keys():
  print ("The name of the file: " , fn)
  print ("The size of the file: ", len(uploaded[fn]))

print ("---------------------------------------------------------------------------------")
# import the dataset
print("importing the dataset ... ")
meregedfile_df = pd.read_csv('final_Dataset_manual2.csv')
print ("done!")
print ("---------------------------------------------------------------------------------")

# print the title of columns
print("print the title of columns: ")
print(meregedfile_df.columns.values.tolist())
print ("---------------------------------------------------------------------------------")

# print the number of columns
print ("The number of the columns: " , len(meregedfile_df.columns))
print ("---------------------------------------------------------------------------------")

# counts and prints the label column values
print("counts and prints the label column values: ")
meregedfile_df['category'].value_counts()

Category = ['commuting', 'entertainment', 'education', 'shopping', 'eating', 'health', 'travel', 
            'work', 'recreation', 'home', 'NONE', 'admin_chores', 'religious', 'police']
Count_Category = [981, 1239, 3732, 1154, 3949, 602, 1832, 1291, 1132, 7921, 2867, 560, 110, 26]

import matplotlib.pyplot as plt
plt.scatter(Count_Category, Category, c='red', marker='^')

#add title
plt.title('Category and the number of category')

#add x and y labels
plt.xlabel('Count_Category')
plt.ylabel('Category')

#show plot
plt.show()

import seaborn as sns

columns = [0,1]
data = meregedfile_df.iloc[:, columns]
sns.jointplot(x='activity_time',y='category',data=data, color='blue', kind='kde')

total_activity_time = []
temp = 0
for i in range(1, 15):
  for j in range(0, len(meregedfile_df)):
    if meregedfile_df.iloc[j,1] == i:
      temp = temp + meregedfile_df.iloc[j,0]
  total_activity_time.append(temp)
  temp = 0

total_activity_time

import matplotlib.pyplot as plt
data = {"Category":['commuting', 'entertainment', 'education', 'shopping', 'eating', 'health', 'travel', 
            'work', 'recreation', 'home', 'NONE', 'admin_chores', 'religious', 'police'],

        "total_activity_time":[3985270.1330000046,  5180592.305000009,  43514188.15900005,  2461681.2770000007,  15582463.187000025,
                                3235427.8789999993,  11618356.395000001,  19248967.268000003,  5366680.60500001,  267315539.81999958,
                                  34522321.579,  1244526.79,  416860.1180000001,  192365.73900000006] };

# Dictionary loaded into a DataFrame       
dataFrame = pd.DataFrame(data=data);

# Draw a vertical bar chart
dataFrame.plot.bar(x="Category", y="total_activity_time", rot=70, title="Category and total of activity time");
plt.show(block=True);

Category = ['commuting', 'entertainment', 'education', 'shopping', 'eating', 'health', 'travel', 
            'work', 'recreation', 'home', 'NONE', 'admin_chores', 'religious', 'police']

import matplotlib.pyplot as plt
plt.scatter(total_activity_time, Category, c='green', marker='o')

#add title
plt.title('Category and total of activity time')

#add x and y labels
plt.xlabel('total_activity_time')
plt.ylabel('Category')

#show plot
plt.show()

total_activity_time_mode = []
temp = 0
for i in range(1, 13):
  for j in range(0, len(meregedfile_df)):
    if meregedfile_df.iloc[j,3] == i:
      temp = temp + meregedfile_df.iloc[j,0]
  total_activity_time_mode.append(temp)
  temp = 0

total_activity_time_mode

mode = ['unknown_activity_type', 'in_vehicle', 'walking', 'cycling', 'in_passenger_vehicle',
            'in_train', 'in_tram', 'in_bus', 'in_subway', 'motor4', 'flying', 'running']

import matplotlib.pyplot as plt
plt.scatter(total_activity_time_mode, mode, c='blue', marker='o')

#add title
plt.title('mode and total of activity time')

#add x and y labels
plt.xlabel('total_activity_time')
plt.ylabel('mode')

#show plot
plt.show()

import matplotlib.pyplot as plt
data = {"mode":['unknown_activity_type', 'in_vehicle', 'walking', 'cycling', 'in_passenger_vehicle',
            'in_train', 'in_tram', 'in_bus', 'in_subway', 'motor4', 'flying', 'running'],

        "total_activity_time_mode":[9774694.154000008, 34231551.14600003,  138369792.19200015,  18107246.357999988,  157365583.7889993,
 3487267.8089999985,  19259558.98399997,  16258473.735,  10271786.816999992,  4052108.016,  2065101.7210000001,  642076.533] };

# Dictionary loaded into a DataFrame       
dataFrame = pd.DataFrame(data=data);

# Draw a vertical bar chart
dataFrame.plot.bar(x="mode", y="total_activity_time_mode",  title="mode and total of activity time");
plt.show(block=True);